{
  "name": "sharehog",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute sharehog-db --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute sharehog-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  },
  "dependencies": {
    "posthog-node": "^5.52.5"
  }
}
